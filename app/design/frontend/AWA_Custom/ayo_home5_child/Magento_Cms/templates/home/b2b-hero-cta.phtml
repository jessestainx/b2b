<?php

declare(strict_types=1);

/** @var \Magento\Framework\View\Element\Template $block */

$whatsappText = rawurlencode((string) __('Olá, quero saber sobre condições B2B na AWA Motos.'));
$whatsappUrl = 'https://wa.me/5516997367588?text=' . $whatsappText;
$registerUrl = $block->getUrl('b2b/register');

/** @var list<array{title: string, text: string, href: string|null, external: bool, icon: string}> $benefits */
$benefits = [
    [
        'title' => (string) __('Preços de atacado'),
        'text' => (string) __('Tabela exclusiva após aprovação do CNPJ'),
        'href' => null,
        'external' => false,
        'icon' => 'price',
    ],
    [
        'title' => (string) __('Catálogo amplo'),
        'text' => (string) __('Peças para reposição com frete para todo o Brasil'),
        'href' => null,
        'external' => false,
        'icon' => 'catalog',
    ],
    [
        'title' => (string) __('Pedido ágil'),
        'text' => (string) __('Listas de compra, cotações e nota fiscal automática'),
        'href' => null,
        'external' => false,
        'icon' => 'order',
    ],
    [
        'title' => (string) __('Condições B2B'),
        'text' => (string) __('Limite de crédito, boleto e vendedor no WhatsApp'),
        'href' => $whatsappUrl,
        'external' => true,
        'icon' => 'whatsapp',
    ],
];

$renderBenefitIcon = static function (string $icon): string {
    $svgAttrs = ' width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" focusable="false"';
    return match ($icon) {
        'catalog' => '<svg' . $svgAttrs . '><path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/></svg>',
        'order' => '<svg' . $svgAttrs . '><path d="M9 5H7a2 2 0 0 0-2 2v12a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2V7a2 2 0 0 0-2-2h-2"/><rect x="9" y="3" width="6" height="4" rx="1"/><path d="M9 12h6"/><path d="M9 16h4"/></svg>',
        'whatsapp' => '<svg' . $svgAttrs . '><path d="M21 11.5a8.38 8.38 0 0 1-9 8.4 8.5 8.5 0 0 1-4.1-1.2L3 20l1.3-4.5a8.47 8.47 0 1 1 16.7-4z"/><path d="M8.5 9.5c.6 2 2 3.4 4 4l1.2-1.2c.2-.2.5-.3.8-.2 1 .3 2 .5 3 .5"/></svg>',
        default => '<svg' . $svgAttrs . '><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>',
    };
};
?>

<section class="awa-hero-b2b-cta awa-section" aria-label="<?= $block->escapeHtmlAttr(__('Cadastro e benefícios B2B')) ?>">
    <div class="awa-hero-b2b-cta__inner container awa-benefits__container awa-section__container">
        <ul class="awa-hero-benefits awa-benefits__grid" aria-label="<?= $block->escapeHtmlAttr(__('Benefícios B2B AWA Motos')) ?>">
            <?php foreach ($benefits as $benefit): ?>
                <?php
                $isLink = ($benefit['href'] ?? null) !== null && ($benefit['href'] ?? '') !== '';
                $itemClass = 'awa-hero-benefits__item awa-benefits__item' . ($isLink ? ' awa-hero-benefits__item--interactive' : '');
                ?>
                <li class="<?= $block->escapeHtmlAttr($itemClass) ?>">
                    <?php if ($isLink): ?>
                        <a href="<?= $block->escapeUrl((string) $benefit['href']) ?>"
                           class="awa-hero-benefits__link awa-benefits__link"
                           <?php if ($benefit['external']): ?>
                               target="_blank"
                               rel="noopener noreferrer"
                           <?php endif; ?>>
                    <?php endif; ?>
                    <span class="awa-hero-benefits__icon awa-benefits__icon" aria-hidden="true">
                        <?= /* @noEscape */ $renderBenefitIcon((string) ($benefit['icon'] ?? 'price')) ?>
                    </span>
                    <span class="awa-hero-benefits__copy awa-benefits__copy">
                        <strong class="awa-hero-benefits__title awa-benefits__title"><?= $block->escapeHtml($benefit['title']) ?></strong>
                        <span class="awa-hero-benefits__text awa-benefits__text"><?= $block->escapeHtml($benefit['text']) ?></span>
                    </span>
                    <?php if ($isLink && $benefit['external']): ?>
                        <?php /* A11Y-001: aviso "abre em nova aba" como texto real (nao aria-label)
                           — o aria-label anterior nao continha o texto visivel (title/text) do
                           link e falhava label-content-name-mismatch. Com .sr-only o nome
                           acessivel passa a ser formado pelo proprio conteudo visivel + este
                           complemento, sem risco de divergencia. */ ?>
                        <span class="sr-only"><?= $block->escapeHtml(__('(abre em nova aba)')) ?></span>
                    <?php endif; ?>
                    <?php if ($isLink): ?>
                        </a>
                    <?php endif; ?>
                </li>
            <?php endforeach; ?>
        </ul>
        <div class="awa-hero-b2b-cta__actions">
            <a class="awa-hero-b2b-cta__button action primary"
               href="<?= $block->escapeUrl($registerUrl) ?>">
                <?= $block->escapeHtml(__('Quero ser revendedor B2B')) ?>
            </a>
        </div>
    </div>
</section>
