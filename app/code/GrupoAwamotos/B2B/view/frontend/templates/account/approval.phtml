<?php
/** @var \GrupoAwamotos\B2B\Block\Account\Approval $block */
$pendingApprovals = $block->getPendingApprovals();
$myApprovals = $block->getMyApprovals();
$canApprove = $block->canApprove();
$approvalWidgetConfig = [
    'actionUrl' => $block->getActionUrl(),
    'messages' => [
        'rejectReasonPrompt' => (string) __('Descreva o motivo da rejeição (obrigatório):'),
        'processError' => (string) __('Não foi possível registrar a decisão. Tente novamente.'),
    ],
];
?>
<div class="b2b-approval-page b2b-page-shell"
     data-mage-init='<?= /* @noEscape */ json_encode(['GrupoAwamotos_B2B/js/account/approval-actions' => $approvalWidgetConfig]) ?>'>
    <div class="b2b-section-header">
        <h1><?= $block->escapeHtml(__('Aprovações de Pedidos')) ?></h1>
        <p class="section-subtitle"><?= $block->escapeHtml(__('Liberar ou recusar pedidos que exigem aprovação da empresa.')) ?></p>
        <?php if ($canApprove): ?>
            <span class="badge badge--approver"><?= $block->escapeHtml(__('Perfil aprovador')) ?></span>
        <?php endif; ?>
    </div>

    <?php if ($canApprove && $pendingApprovals->getSize() > 0): ?>
    <!-- Pedidos Aguardando Sua Aprovação -->
    <div class="approval-section">
        <h2><?= __('Aguardando Sua Aprovação') ?> <span class="count">(<?= (int)$pendingApprovals->getSize() ?>)</span></h2>
        <table class="data table table-approvals">
            <thead>
                <tr>
                    <th><?= __('Pedido') ?></th>
                    <th><?= __('Valor') ?></th>
                    <th><?= __('Nível Atual') ?></th>
                    <th><?= __('Nível Requerido') ?></th>
                    <th><?= __('Data') ?></th>
                    <th><?= __('Ações') ?></th>
                </tr>
            </thead>
            <tbody>
                <?php foreach ($pendingApprovals as $approval): ?>
                <tr>
                    <td data-th="<?= $block->escapeHtmlAttr(__('Pedido')) ?>">
                        <?= $block->escapeHtml($block->getOrderIncrementId((int)$approval->getData('order_id'))) ?>
                    </td>
                    <td data-th="<?= $block->escapeHtmlAttr(__('Valor')) ?>">
                        <?= /* @noEscape */ $block->formatPrice((float)$approval->getData('order_total')) ?>
                    </td>
                    <td data-th="<?= $block->escapeHtmlAttr(__('Nível')) ?>">
                        <?= $block->escapeHtml($block->getLevelLabel((int)$approval->getData('current_level'))) ?>
                    </td>
                    <td data-th="<?= $block->escapeHtmlAttr(__('Requerido')) ?>">
                        <?= $block->escapeHtml($block->getLevelLabel((int)$approval->getData('required_level'))) ?>
                    </td>
                    <td data-th="<?= $block->escapeHtmlAttr(__('Data')) ?>">
                        <?= $block->escapeHtml(date('d/m/Y H:i', strtotime($approval->getData('created_at')))) ?>
                    </td>
                    <td data-th="<?= $block->escapeHtmlAttr(__('Ações')) ?>" class="approval-actions">
                        <button type="button" class="btn-approve"
                                data-action="approve"
                                data-approval-id="<?= (int) $approval->getId() ?>">
                            <?= __('Aprovar') ?>
                        </button>
                        <button type="button" class="btn-reject"
                                data-action="reject"
                                data-approval-id="<?= (int) $approval->getId() ?>">
                            <?= __('Rejeitar') ?>
                        </button>
                    </td>
                </tr>
                <?php endforeach; ?>
            </tbody>
        </table>
    </div>
    <?php endif; ?>

    <!-- Meus Pedidos em Aprovação -->
    <div class="approval-section">
        <h2><?= __('Meus Pedidos em Aprovação') ?></h2>
        <?php if ($myApprovals->getSize() > 0): ?>
        <table class="data table table-my-approvals">
            <thead>
                <tr>
                    <th><?= __('Pedido') ?></th>
                    <th><?= __('Valor') ?></th>
                    <th><?= __('Status') ?></th>
                    <th><?= __('Nível Atual') ?></th>
                    <th><?= __('Data') ?></th>
                </tr>
            </thead>
            <tbody>
                <?php foreach ($myApprovals as $approval): ?>
                <tr>
                    <td data-th="<?= $block->escapeHtmlAttr(__('Pedido')) ?>">
                        <?= $block->escapeHtml($block->getOrderIncrementId((int)$approval->getData('order_id'))) ?>
                    </td>
                    <td data-th="<?= $block->escapeHtmlAttr(__('Valor')) ?>">
                        <?= /* @noEscape */ $block->formatPrice((float)$approval->getData('order_total')) ?>
                    </td>
                    <td data-th="<?= $block->escapeHtmlAttr(__('Status')) ?>">
                        <span class="approval-status approval-status--<?= $block->escapeHtmlAttr($approval->getData('status')) ?>">
                            <?= $block->escapeHtml($block->getStatusLabel($approval->getData('status'))) ?>
                        </span>
                    </td>
                    <td data-th="<?= $block->escapeHtmlAttr(__('Nível')) ?>">
                        <?= $block->escapeHtml($block->getLevelLabel((int)$approval->getData('current_level'))) ?>
                    </td>
                    <td data-th="<?= $block->escapeHtmlAttr(__('Data')) ?>">
                        <?= $block->escapeHtml(date('d/m/Y H:i', strtotime($approval->getData('created_at')))) ?>
                    </td>
                </tr>
                <?php endforeach; ?>
            </tbody>
        </table>
        <?php else: ?>
            <p class="empty-message"><?= $block->escapeHtml(__('Nenhum pedido seu aguarda aprovação no momento.')) ?></p>
        <?php endif; ?>
    </div>
</div>
