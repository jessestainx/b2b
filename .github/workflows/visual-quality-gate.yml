# STATUS: QUARENTENA — NÃO EXECUTAR (Fase 1)
# Ticket: CI-CONTAINMENT-FASE1-2026-07-22
#
# Motivo: vars.PLAYWRIGHT_BASE_URL_PR estava configurada como [REDACTED-PROD-HOST]
# (produção). Resolver antigo liberava produção em push/schedule sem ack.
# Input allow_production_validation removido — uma flag simples NÃO reabilita produção.
#
# Reativação (Fase 2+):
#   1) vars apontando SOMENTE para staging isolado (nunca [REDACTED-PROD-HOST] / IP prod)
#   2) environment GitHub com reviewers
#   3) guard estático verde
#   4) aprovação explícita
# Sem workflow_dispatch enquanto produção NO-GO.

name: "Visual QA & Lighthouse Gate"

on:
  workflow_call:

permissions:
  contents: read

jobs:
  quarantine-notice:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Quarantined
        run: |
          echo "STATUS: QUARENTENA — NÃO EXECUTAR"
          echo "Produção NO-GO. Sem Playwright. Sem Lighthouse remoto."
          exit 1
