# STATUS: QUARENTENA — NÃO EXECUTAR
# Ticket: CI-CONTAINMENT-FASE1-2026-07-22
# Motivo: AWA_BASE_URL=[REDACTED-PROD-HOST] (produção hard-coded)
# Required check: NÃO
# Reativação: staging isolado + Playwright via tests/e2e (não npm ad-hoc na raiz)
# Nota: em grupoawamarketing-wq/awa não há permissão admin para disable API; contenção via YAML.

name: menu-regression

on:
  workflow_call:

permissions:
  contents: read

jobs:
  quarantine-notice:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Quarantined
        run: |
          echo "STATUS: QUARENTENA — NÃO EXECUTAR"
          exit 1
