# STATUS: QUARENTENA — NÃO EXECUTAR
# Ticket: CI-CONTAINMENT-FASE1-2026-07-22
# Motivo: npm scripts test:mcp-visual defaultavam produção; faltam tests/e2e/scripts/*.mjs
# Caminhos ausentes (não criar stubs nesta fase):
#   - tests/e2e/scripts/mcp-visual-report.mjs
#   - tests/e2e/scripts/mcp-visual-autofix.mjs
#   - tests/e2e/scripts/visual-baseline-guard.mjs
# Contenção API: disabled_manually (2026-07-22). Sem workflow_dispatch (NO-GO produção).

name: e2e-nightly-full

on:
  workflow_call:

permissions:
  contents: read

jobs:
  quarantine-notice:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Quarantined
        run: |
          echo "STATUS: QUARENTENA — NÃO EXECUTAR"
          exit 1
