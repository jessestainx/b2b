# STATUS: QUARENTENA — NÃO EXECUTAR (Fase 1)
# Ticket: CI-CONTAINMENT-FASE1-2026-07-22
#
# Motivo: caminhos de PR/dispatch podiam resolver BASE_URL para [REDACTED-PROD-HOST]
# (vars/secrets). Input allow_production_validation removido.
#
# Reativação: staging only + environment aprovado + guard estático + ticket.
# Config Playwright válida: tests/e2e/playwright.config.ts (não a raiz).

name: Playwright Visual QA

on:
  workflow_call:

permissions:
  contents: read

jobs:
  quarantine-notice:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Quarantined
        run: |
          echo "STATUS: QUARENTENA — NÃO EXECUTAR"
          exit 1
