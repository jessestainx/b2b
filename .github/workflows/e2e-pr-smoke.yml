# STATUS: QUARENTENA — NÃO EXECUTAR
# Ticket: CI-CONTAINMENT-FASE1-2026-07-22
# Motivo: apontava para [REDACTED-PROD-HOST] em PR + secrets B2B + ALLOW_PRODUCTION_VALIDATION(true)
# Required check: NÃO (ver GITHUB_ACTIONS_CONTAINMENT_2026-07-22.md)
# Reativação: staging isolado + environment aprovado + guard estático verde + aprovação explícita Fase 2+
# Não usar workflow_dispatch enquanto produção permanecer NO-GO.
#
# Contenção adicional: workflow desabilitado via API GitHub (disabled_manually) em
# awamotosbrand-prog/magento_b2b_awa e ecriativy-git/b2b (2026-07-22).

name: e2e-pr-smoke

# Triggers automáticos removidos. workflow_call sem callers = arquivo preservado, não executa.
on:
  workflow_call:

permissions:
  contents: read

jobs:
  quarantine-notice:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Quarantined
        run: |
          echo "STATUS: QUARENTENA — NÃO EXECUTAR"
          exit 1
