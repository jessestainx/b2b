# STATUS: QUARENTENA — NÃO EXECUTAR
# Ticket: CI-CONTAINMENT-FASE1-2026-07-22
# Motivo: pipeline:mcp-visual / test:b2b-regression:all dependem de defaults/prod e scripts .mjs ausentes
# Caminhos ausentes: tests/e2e/scripts/{mcp-visual-report,mcp-visual-autofix,visual-baseline-guard}.mjs
# Contenção API: disabled_manually (2026-07-22). Sem workflow_dispatch (NO-GO produção).

name: e2e-premerge-regression

on:
  workflow_call:

permissions:
  contents: read

jobs:
  quarantine-notice:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Quarantined
        run: |
          echo "STATUS: QUARENTENA — NÃO EXECUTAR"
          exit 1
