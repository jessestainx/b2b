# STATUS: QUARENTENA — NÃO EXECUTAR
# Ticket: CI-CONTAINMENT-FASE1-2026-07-22
# Motivo: scaffold genérico; npm ci na raiz SEM @playwright/test; artefato playwright-report/ desalinhado
# Configuração válida: tests/e2e/ (package.json + playwright.config.ts + pw-*.config.ts)
# Não instalar Playwright na raiz só para “passar”. Não executar browser.
# Contenção API: disabled_manually em remotes com admin (2026-07-22).

name: Playwright Tests

on:
  workflow_call:

permissions:
  contents: read

jobs:
  quarantine-notice:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Quarantined
        run: |
          echo "STATUS: QUARENTENA — NÃO EXECUTAR"
          echo "Use tests/e2e — não a raiz."
          exit 1
